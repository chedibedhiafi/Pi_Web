{% extends 'base.html.twig' %}

{% block title %}Reservation{% endblock %}

{% block body %}
    <h1>Reservation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>ReservId</th>
                <td>{{ reservation.reservId }}</td>
            </tr>
            <tr>
                <th>NbPlaces</th>
                <td>{{ reservation.nbPlaces }}</td>
            </tr>
            <tr>
                <th>DateAchat</th>
                <td>{{ reservation.dateAchat ? reservation.dateAchat|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Total</th>
                <td>{{ reservation.total }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_reservation_index') }}">Retour à la liste</a>

    <a href="{{ path('app_reservation_edit', {'reservId': reservation.reservId}) }}">Modifier</a>

    {{ include('reservation/_delete_form.html.twig') }}
{% endblock %}
