{{ form_start(form) }}
{{ form_row(form.q)}}
<h4>Catégories</h4>
{{ form_row(form.categories)}}
<h4>Prix</h4>
<div class="row">
    {{ form_row(form.min) }}
</div>
<div class="row">
    {{ form_row(form.max) }}
</div>

<button type="submit" class="btn rounded-pill btn-primary">Rechercher</button>
{{ form_end(form) }}