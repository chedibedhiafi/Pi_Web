{% extends 'front.html.twig' %}

{% block title %}Login{% endblock %}

{% block home %}
{% endblock %}

{% block aboutUs %}
{% endblock %}

{% block services %}
{% endblock %}

{% block portfolio %}
{% endblock %}

{% block team %}
{% endblock %}

{% block pricing %}
{% endblock %}

{% block faq %}
{% endblock %}

{% block contactus %}

<div class="container rounded bg-white mt-5 mb-5">
    <div class="section-title" data-aos="fade-up">
        <h2>Mon profil</h2>
    </div>

    <div class="row">
        <div class="col-md-2 border-center">
            <div class="d-flex flex-column align-items-center text-center p-3 py-5"><img class="rounded-circle mt-5" width="150px" src="{{asset('img/avatar-default-icon.png')}}"><span class="font-weight-bold">{{app.user.prenom}} {{app.user.nom}}</span></div>
        </div>
        <div class="col-md-5 border-center">
            <div class="p-3 py-5">
            <form method="post" path="{{path('app_utilisateur_editprofil', {'id':app.user.id})}}">
                <div class="row mt-1">
                    <div class="col-md-6"><label class="labels">Nom</label><input type="text" class="form-control" name="nom" placeholder="{{app.user.nom}}" value=""></div>
                    <div class="col-md-6"><label class="labels">Prénom</label><input type="text" class="form-control" name="prenom" value="" placeholder="{{app.user.prenom}}"></div>
                </div>
                <div class="row mt-2">
                    <div class="col-md-12"><label class="labels">Login</label><input disabled type="text" class="form-control" name="login" placeholder="{{app.user.username}}" value=""></div>
                    <div class="col-md-12"><label class="labels">Date de naissance</label><input disabled type="text" class="form-control" name="dateN" placeholder="{{app.user.dateNaissance|date("m/d/Y")}}"></div>
                    <div class="col-md-12"><label class="labels">Adresse mail</label><input disabled type="text" class="form-control" name="email" placeholder="{{app.user.email}}" value=""></div>
                </div>
                <div class="mt-5 text-center"><button type="submit" class="btn btn-primary profile-button">Enregistrer</button></div>
                </form>
            </div>
        </div>   
    </div>

</div>



{% endblock %}