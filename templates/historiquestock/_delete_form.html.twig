<form method="post" action="{{ path('app_historiquestock_delete', {'reference': historiquestock.reference}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ historiquestock.reference) }}">
    <button class="btn">Delete</button>
</form>
